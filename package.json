{
  "name": "xianjian",
  "version": "0.1.0",
  "private": true,
  "description": "仙剑奇侠传·逍遥游 - 在线卡牌对战框架（Node.js + Vue3 + better-sqlite3）",
  "scripts": {
    "setup": "cd server && npm install && cd ../client && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently -n 服务端,客户端 -c green,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
